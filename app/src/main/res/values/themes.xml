<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal base theme: the real graphical theme is handled by Jetpack Compose -->
    <style name="Theme.NinnaNanna" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>